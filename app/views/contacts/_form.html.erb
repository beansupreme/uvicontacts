<%= form_for(@contact) do |f| %>
  <% if @contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
      <% @contact.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :telephone %><br>
    <%= f.text_field :telephone, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :emergency_contact_name %><br>
    <%= f.text_field :emergency_contact_name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :emergency_contact_telephone %><br>
    <%= f.text_field :emergency_contact_telephone, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit 'Submit', class: 'btn btn-primary', type: 'submit'  %>
  </div>
<% end %>
